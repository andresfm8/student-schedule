import { createStyles, makeStyles } from "@mui/styles";

const useStyles = makeStyles(
  createStyles({
    yearTermContainer: {
      marginBottom: '0.5em',
    },
  }),
);

export default useStyles;